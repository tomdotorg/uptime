hot reload config w/out network change
concurrent checks
change interval on the fly
show uptime and maybe more info in the header
for just hosts, try 80, 443, 22, 53?
detect dups in config

terminalize it like watch or top
